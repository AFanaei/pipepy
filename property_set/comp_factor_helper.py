

def calculate_comp_factor(P, T, composition, component_list):
    # P: numpy array of pressure (bar)
    # T: numpy array of temperature (centigerad)
    # composition: array of molar composition sum(composition)==1
    # component_list: class

    return [1] * len(P)
